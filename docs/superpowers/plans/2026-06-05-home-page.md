# 首頁重設計（混合版）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** 把首頁重建為設計稿的「商品導向」混合版——保留現有品牌故事/地圖/CTA 的特色內容，hero 改設計稿輪播風格，插入「精選商品」與「本季新品」真實商品區，並新增 `is_featured` 策展能力（migration + admin 開關）。

**Architecture:** 沿用既有 Supabase server-fetch 樣板與設計 token/layout（phase 1 已落地）。新增共用 `ProductCard`（從 `ProductsList.tsx` inline markup 抽出）、`src/lib/products.ts` 資料存取 helper、`is_featured` 欄位（nullable migration）與 admin 開關。首頁為 server component，一次 `Promise.all` 抓精選 + 新品。

**Tech Stack:** Next.js 16 RSC、Supabase（`createServerSupabase`）、Tailwind 4 + shadcn、lucide-react、既有 `formatPrice`/`getProductImageUrl`/`WishlistToggle`。

**決策（使用者確認）：** 首頁=混合（保留品牌故事+加商品區）；精選=加 `is_featured` 欄位 + admin 開關。

**外部相依（重要）：** `is_featured` migration 檔本 PR 提供，但需由使用者套用到 Supabase（`supabase db push` 或 dashboard）。精選商品查詢必須**防禦性**：欄位不存在或查詢失敗 → 回空陣列、首頁該區塊隱藏，不可讓整頁 500。

**前置：** worktree `.worktrees/feat/home-page`（基於 origin/dev，含 phase 1）。指令在 `nexbuy-web/`。動 Next 前可查 `node_modules/next/dist/docs/`。Commit trailer：`Co-authored-by: Claude <claude@anthropic.com>`。先讀 `docs/conventions.md`（migration 政策、server action 樣板、金額 cents）。

---

## 檔案結構

| 檔案 | 動作 | 責任 |
|---|---|---|
| `supabase/migrations/20260605000000_product_is_featured.sql` | 新 | `add column if not exists is_featured boolean`（nullable） |
| `src/lib/types/database.ts` | 改 | `Product` 加 `is_featured: boolean \| null` |
| `src/lib/schemas/product.ts` | 改 | `baseShape` 加 `is_featured: z.coerce.boolean().optional()` |
| `src/lib/products.ts` | 新 | `getNewArrivals(n)`、`getFeaturedProducts(n)`（防禦性）、共用 select 欄位常數、`ProductCardData` type |
| `src/components/site/ProductCard.tsx` | 新 | 共用商品卡（圖/名/價/kind badge/缺貨/wishlist），從 ProductsList 抽出 |
| `src/app/products/ProductsList.tsx` | 改 | 改用 `<ProductCard>`（DRY；保留既有篩選/排序行為） |
| `src/app/page.tsx` | 改 | 混合首頁：reshape hero + 精選/新品區 + 試戴/測驗導引 + 處方 CTA；保留品牌故事/地圖/CTA |
| `src/app/admin/(protected)/products/[id]/edit/page.tsx` + 其 server action | 改 | 表單加 is_featured 開關 + update payload |
| `src/app/admin/(protected)/products/new/page.tsx` + 其 server action | 改 | 表單加 is_featured 開關 + insert payload |

---

## Task 1: 資料基礎（migration + type + schema + products helper）

**Files:** migration（新）、`src/lib/types/database.ts`、`src/lib/schemas/product.ts`、`src/lib/products.ts`（新）

- [ ] **Step 1: migration 檔**

Create `supabase/migrations/20260605000000_product_is_featured.sql`:
```sql
-- 首頁「精選商品」策展旗標。依 migration 政策：nullable、無 default、不 backfill。
alter table products
  add column if not exists is_featured boolean;

comment on column products.is_featured is '首頁精選商品策展旗標；null/false = 不精選';

-- 精選查詢索引（部分索引，只索引 featured=true 的列）
create index if not exists products_is_featured_idx
  on products (is_featured)
  where is_featured = true;
```

- [ ] **Step 2: Product type**

In `src/lib/types/database.ts`, add to the `Product` interface (after `try_on_image_url`):
```ts
  is_featured: boolean | null;
```

- [ ] **Step 3: zod schema**

In `src/lib/schemas/product.ts` `baseShape`, add (near `is_online_available`):
```ts
  is_featured: z.coerce.boolean().optional(),
```

- [ ] **Step 4: products helper**

Create `src/lib/products.ts`:
```ts
import { createServerSupabase } from "@/lib/supabase/server";

// 首頁商品卡需要的欄位（比列表頁精簡，省 payload）。
export const PRODUCT_CARD_COLUMNS =
  "id, slug, name, price_cents, image_urls, kind, finished_stock, is_online_available";

export type ProductCardData = {
  id: string;
  slug: string;
  name: string;
  price_cents: number;
  image_urls: string[];
  kind: "finished" | "prescription_frame";
  finished_stock: number | null;
  is_online_available: boolean;
};

/** 本季新品：依 created_at 由新到舊。 */
export async function getNewArrivals(limit = 8): Promise<ProductCardData[]> {
  const sb = await createServerSupabase();
  const { data, error } = await sb
    .from("products")
    .select(PRODUCT_CARD_COLUMNS)
    .eq("is_online_available", true)
    .order("created_at", { ascending: false })
    .limit(limit);
  if (error) {
    console.error("getNewArrivals failed:", error);
    return [];
  }
  return (data ?? []) as ProductCardData[];
}

/**
 * 精選商品：is_featured = true。
 * 防禦性：is_featured migration 尚未套用時查詢會報錯 → 回空陣列，
 * 首頁該區塊自行隱藏，不可讓整頁壞掉。
 */
export async function getFeaturedProducts(limit = 8): Promise<ProductCardData[]> {
  const sb = await createServerSupabase();
  const { data, error } = await sb
    .from("products")
    .select(PRODUCT_CARD_COLUMNS)
    .eq("is_online_available", true)
    .eq("is_featured", true)
    .order("created_at", { ascending: false })
    .limit(limit);
  if (error) {
    // 欄位不存在（migration 未套）或其他錯誤 → graceful fallback
    console.warn("getFeaturedProducts unavailable (is_featured migration applied?):", error.message);
    return [];
  }
  return (data ?? []) as ProductCardData[];
}
```

- [ ] **Step 5: verify** `pnpm typecheck`（PASS）、`pnpm lint`（無新錯誤）。

- [ ] **Step 6: commit**
```
git add supabase/migrations/20260605000000_product_is_featured.sql src/lib/types/database.ts src/lib/schemas/product.ts src/lib/products.ts
git commit -m "feat(products): add is_featured column + products data helpers (new arrivals / featured)

Co-authored-by: Claude <claude@anthropic.com>"
```

---

## Task 2: 共用 ProductCard 元件

**Files:** `src/components/site/ProductCard.tsx`（新）、`src/app/products/ProductsList.tsx`（改用）

- [ ] **Step 1: 讀現有 inline 卡片** — 開 `src/app/products/ProductsList.tsx`，找到 product `<li><Link>...<Card>...` 那段（約 154–202 行），理解它用到 `getProductImageUrl`、`formatPrice`、`WishlistToggle`、shadcn `Card/CardHeader/CardTitle/CardContent/CardFooter`、`Badge`、`next/image`。確認 import 路徑。

- [ ] **Step 2: 建 `src/components/site/ProductCard.tsx`**（client component，因含 WishlistToggle 互動；接受卡片資料 + wishlist 狀態）：
```tsx
"use client";

import Link from "next/link";
import Image from "next/image";
import { Card, CardHeader, CardTitle, CardContent, CardFooter } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { WishlistToggle } from "@/app/products/WishlistToggle";
import { getProductImageUrl } from "@/lib/product-placeholder";
import { formatPrice } from "@/lib/format";

export type ProductCardProduct = {
  id: string;
  slug: string;
  name: string;
  price_cents: number;
  image_urls: string[];
  kind: "finished" | "prescription_frame";
  finished_stock?: number | null;
  description?: string | null;
};

export function ProductCard({
  product,
  inWishlist = false,
  isLoggedIn = false,
  priority = false,
}: {
  product: ProductCardProduct;
  inWishlist?: boolean;
  isLoggedIn?: boolean;
  priority?: boolean;
}) {
  const soldOut =
    product.kind === "finished" && (product.finished_stock ?? 0) <= 0;

  return (
    <Link href={`/products/${product.slug}`} className="group block">
      <Card className="h-full overflow-hidden transition-shadow hover:shadow-2">
        <div className="relative aspect-square overflow-hidden rounded-t-lg bg-muted">
          <Image
            src={getProductImageUrl(product)}
            alt={product.name}
            fill
            priority={priority}
            sizes="(min-width: 1024px) 25vw, (min-width: 640px) 50vw, 100vw"
            unoptimized={!product.image_urls[0]}
            className={`object-cover transition-transform duration-300 group-hover:scale-105 ${
              soldOut ? "opacity-60 grayscale" : ""
            }`}
          />
          {soldOut && (
            <span className="absolute left-2 top-2 rounded-sm bg-foreground/80 px-2 py-0.5 text-[11px] font-medium text-background">
              缺貨
            </span>
          )}
          <WishlistToggle
            productId={product.id}
            initialInWishlist={inWishlist}
            isLoggedIn={isLoggedIn}
            variant="heart"
          />
        </div>
        <CardHeader>
          <div className="flex items-start justify-between gap-2">
            <CardTitle className="font-serif text-base leading-tight">
              {product.name}
            </CardTitle>
            <Badge variant={product.kind === "finished" ? "default" : "outline"} className="shrink-0">
              {product.kind === "finished" ? "成品" : "預約配鏡"}
            </Badge>
          </div>
        </CardHeader>
        {product.description != null && (
          <CardContent className="text-sm text-muted-foreground line-clamp-2">
            {product.description}
          </CardContent>
        )}
        <CardFooter>
          <span className="font-display text-lg font-semibold text-primary">
            {formatPrice(product.price_cents)}
          </span>
        </CardFooter>
      </Card>
    </Link>
  );
}
```
> 確認 `WishlistToggle` 的 import 路徑（在 `src/app/products/` 下；用 git grep 確認 export 名與 props）。若它不是 default export 或路徑不同，依實際調整。`hover:shadow-2` 用 phase1 的 shadow token；若無此 utility，改 `hover:shadow-lg`。

- [ ] **Step 3: ProductsList 改用 ProductCard** — 把 `ProductsList.tsx` 內那段 inline `<Card>...</Card>` 換成 `<li key={p.id}><ProductCard product={p} inWishlist={wishlistIds.includes(p.id)} isLoggedIn={isLoggedIn} priority={idx < 3} /></li>`。移除因此不再用到的 import（Card 系列、Badge、Image、getProductImageUrl、formatPrice、WishlistToggle——僅移除 ProductsList 內確實不再使用的）。保留篩選/排序/空狀態邏輯不動。

- [ ] **Step 4: verify** `pnpm typecheck`、`pnpm lint`（無新錯誤）、`pnpm build`（/products 正常）。手動確認 ProductsList 行為未變（卡片內容一致）。

- [ ] **Step 5: commit**
```
git add src/components/site/ProductCard.tsx src/app/products/ProductsList.tsx
git commit -m "refactor(products): extract shared ProductCard; ProductsList uses it

Co-authored-by: Claude <claude@anthropic.com>"
```

---

## Task 3: 首頁混合重組（hero + 商品區 + 導引）

**Files:** `src/app/page.tsx`

- [ ] **Step 1:** 讀現有 `src/app/page.tsx`（保留：品牌故事 `#story`、地圖 `#visit`、Footer CTA `#cta`、JsonLd）。改動如下：

- [ ] **Step 2: server fetch** — 在 `HomePage` 改為 `async`，最上方：
```tsx
import { getFeaturedProducts, getNewArrivals } from "@/lib/products";
import { ProductCard } from "@/components/site/ProductCard";
import { getWishlistProductIds } from "@/lib/wishlist";
import { createServerSupabase } from "@/lib/supabase/server";
// ...
export default async function HomePage() {
  const [featured, newArrivals, wishlistIds, { data: { user } }] = await Promise.all([
    getFeaturedProducts(8),
    getNewArrivals(8),
    getWishlistProductIds(),
    (await createServerSupabase()).auth.getUser(),
  ]);
  const isLoggedIn = !!user;
  // ...
```
> 確認 `getWishlistProductIds` 的 export/簽章（Task 2 已查 WishlistToggle；用同模組）。若它已內部建立 supabase client，避免重複；可分開 await。

- [ ] **Step 3: reshape hero** — 把現有 hero 文案區換成設計稿風格（保留輪播 `HeroCarousel`）：mono eyebrow `ARTISAN · EYEWEAR · 2026`（`.eyebrow` class）、serif 大標、副文、兩顆按鈕「選購鏡框」(`/products`)、「先做個臉型測驗」(`/quiz`)。hero 容器改用 `.container`（取代 `max-w-5xl`）。
```tsx
<p className="eyebrow">ARTISAN · EYEWEAR · 2026</p>
<h1 className="font-serif text-4xl font-medium leading-[1.05] tracking-tight text-foreground md:text-6xl">在家挑框<br />到店配鏡</h1>
<p className="text-lg leading-relaxed text-muted-foreground">成品眼鏡線上直接購買；處方鏡架線上預約到店驗光配鏡。</p>
<div className="flex flex-wrap gap-3 pt-2">
  <Link href="/products" className={buttonVariants({ size: "lg" })}>選購鏡框</Link>
  <Link href="/quiz" className={buttonVariants({ size: "lg", variant: "outline" })}>先做個臉型測驗</Link>
</div>
```

- [ ] **Step 4: 精選商品區（防禦性，空則隱藏）** — 插在 hero 之後、品牌故事之前：
```tsx
{featured.length > 0 && (
  <section className="container py-16">
    <div className="mb-8 flex items-end justify-between gap-4">
      <div>
        <p className="eyebrow mb-2">FEATURED</p>
        <h2 className="font-serif text-2xl font-medium md:text-3xl">精選商品</h2>
      </div>
      <Link href="/products" className="inline-flex items-center gap-1 text-sm text-primary hover:underline underline-offset-2">
        看全部 <ArrowRight className="size-3.5" />
      </Link>
    </div>
    <ul className="grid grid-cols-2 gap-4 md:grid-cols-3 nav:grid-cols-4">
      {featured.map((p, i) => (
        <li key={p.id}><ProductCard product={p} inWishlist={wishlistIds.includes(p.id)} isLoggedIn={isLoggedIn} priority={i < 4} /></li>
      ))}
    </ul>
  </section>
)}
```

- [ ] **Step 5: 本季新品區** — 插在品牌故事/服務之後（保留現有 `#story`、`#services`、`#how`）：與精選同結構，eyebrow `NEW ARRIVALS`、標題「本季新品」、資料 `newArrivals`、`{newArrivals.length > 0 && (...)}`。

- [ ] **Step 6: 試戴/測驗導引 + 處方鏡框 CTA** — 現有 `#how`（怎麼運作三步）對應處方三步流程，保留。另在新品區後加一個「試戴/測驗導引」雙卡區（連 `/tryon`、`/quiz`），用 `.container`：
```tsx
<section className="container py-16">
  <div className="grid gap-4 sm:grid-cols-2">
    <Link href="/tryon" className="group rounded-xl border bg-card p-8 transition-colors hover:border-primary/40">
      <Camera className="size-6 text-primary" />
      <h3 className="mt-4 font-serif text-xl font-medium">虛擬試戴</h3>
      <p className="mt-2 text-sm text-muted-foreground">上傳照片，線上套上鏡框看效果。</p>
      <span className="mt-4 inline-flex items-center gap-1 text-sm text-primary">開始試戴 <ArrowRight className="size-3.5" /></span>
    </Link>
    <Link href="/quiz" className="group rounded-xl border bg-card p-8 transition-colors hover:border-primary/40">
      <Sparkles className="size-6 text-primary" />
      <h3 className="mt-4 font-serif text-xl font-medium">臉型測驗</h3>
      <p className="mt-2 text-sm text-muted-foreground">四題找出你的臉型與推薦框型。</p>
      <span className="mt-4 inline-flex items-center gap-1 text-sm text-primary">開始測驗 <ArrowRight className="size-3.5" /></span>
    </Link>
  </div>
</section>
```
（import `Camera`、`Sparkles`、`ArrowRight` from lucide-react。）

- [ ] **Step 7:** 保留 `#story`、`#services`、`#how`、`#visit`、`#cta` 既有區塊與 JsonLd。確保新區塊用 `.container`（1320）與設計 token，與保留區塊（`max-w-5xl`）視覺不至突兀——可將保留區塊外層也漸進改 `.container`（選擇性，若時間允許；非必要）。

- [ ] **Step 8: verify** `pnpm typecheck`、`pnpm lint`（無新錯誤）、`pnpm build`。

- [ ] **Step 9: commit**
```
git add src/app/page.tsx
git commit -m "feat(home): hybrid home — handoff hero + featured/new-arrivals grids + tryon/quiz guides

Co-authored-by: Claude <claude@anthropic.com>"
```

---

## Task 4: Admin is_featured 開關

**Files:** admin product `new` + `[id]/edit` 頁與其 server actions

- [ ] **Step 1:** 讀 `src/app/admin/(protected)/products/new/page.tsx`、`src/app/admin/(protected)/products/[id]/edit/page.tsx` 與它們呼叫的 server action（用 git grep 找 `createProduct`/`updateProduct` 或 `actions.ts`）。理解現有表單欄位（尤其 `is_online_available` 的 checkbox 寫法）與 server action 如何 build payload + 用 `createProductSchema`/`updateProductSchema`。

- [ ] **Step 2:** 在兩個表單加一個 `is_featured` checkbox（沿用 `is_online_available` 的既有 markup 樣式），name=`is_featured`，編輯頁 `defaultChecked={product.is_featured ?? false}`。

- [ ] **Step 3:** server action：schema 已含 `is_featured`（Task 1）。在 insert/update payload 加 `is_featured: parsed.data.is_featured ?? false`。遵守 conventions server-action 樣板（safeParse、`.select()`、0 rows 偵測、revalidate）——沿用該 action 既有結構，僅加欄位。

- [ ] **Step 4: verify** `pnpm typecheck`、`pnpm lint`（無新錯誤）、`pnpm build`。

- [ ] **Step 5: commit**
```
git add src/app/admin
git commit -m "feat(admin): is_featured toggle on product new/edit forms

Co-authored-by: Claude <claude@anthropic.com>"
```

---

## Task 5: 驗證（建置 + smoke）

- [ ] `pnpm build` 全綠（含 / 與 /products 與 admin）。
- [ ] 複製 `.env.local` 進 worktree（gitignored），`pnpm start`，curl 檢查：`/` 200、含 hero eyebrow `ARTISAN`、`本季新品`、（精選若無資料則該區不出現，正常）、品牌故事/地圖/CTA 仍在；`/products` 200 且卡片正常。
- [ ] 確認「精選商品」在 is_featured migration 未套用時**不會讓 / 變 500**（getFeaturedProducts 回空、區塊隱藏）。
- [ ] 視覺/RWD/深色人工驗收：交由使用者最後一起驗。

## Self-Review 對照
- 首頁混合（保留品牌故事+加商品區）→ Task 3 ✓
- is_featured 欄位 + admin 開關 → Task 1（migration/type/schema）+ Task 4（admin）✓
- 共用 ProductCard + ProductsList DRY → Task 2 ✓
- 防禦性 featured 查詢（migration 未套不壞頁）→ Task 1 Step 4 + Task 5 ✓
- 本季新品（created_at）→ Task 1 + Task 3 ✓
- 試戴/測驗導引、處方三步（保留 #how）→ Task 3 ✓

> 外部相依：`20260605000000_product_is_featured.sql` 需使用者套用到 Supabase 後，admin 勾選才會生效、精選商品區才會出現。
